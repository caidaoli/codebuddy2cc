# codebuddy2cc 环境变量配置
# 复制此文件为 .env 并配置实际值

# 必需配置 - 客户端认证token
# 客户端可以使用 "Authorization: Bearer {CODEBUDDY2CC_AUTH}" 头
# 或 "X-API-Key: {CODEBUDDY2CC_AUTH}" 头进行认证
CODEBUDDY2CC_AUTH=your_auth_token_here

# 必需配置 - 上游CodeBuddy API密钥
CODEBUDDY2CC_KEY=your_codebuddy_api_key_here

# 可选配置 - 服务端口（默认8080）
PORT=8080

# 可选配置 - 调试模式（输出详细的请求/响应日志）
# 设置为 true、1 或 on 启用调试模式
DEBUG=false

# 可选配置 - 调试日志文件路径（启用DEBUG时保存调试输出到文件）
# 如果未设置，调试输出仅显示在控制台
DEBUG_FILE=./debug.log

# 可选配置 - 上游API地址（测试用）
# CODEBUDDY2CC_UPSTREAM_URL=https://www.codebuddy.ai/v2/chat/completions

# macOS服务配置说明
# 作为LaunchAgent服务运行时：
# - 服务名称: com.codebuddy2cc.service
# - 标准输出日志: logs/codebuddy2cc.log
# - 错误日志: logs/codebuddy2cc.error.log
# - 服务在用户登录时自动启动
# - 网络可用时自动重启（如果意外退出）
# - 进程类型: Background（后台服务）

# 服务管理命令:
# make install-service    - 安装并启动服务
# make uninstall-service  - 仅卸载服务，保留文件
# make remove-service     - 完全删除服务和所有文件（有确认）
# make remove-service-force - 强制删除服务和所有文件（无确认）
# make check-files        - 检查服务相关文件状态
# make status            - 查看服务运行状态